/**
 * The admin application is the opposite of the public site: never prerendered,
 * fully client-side interactive.
 */
export const prerender = false;
export const ssr = true;
export const csr = true;
